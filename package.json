{
  "private": true,
  "scripts": {
    "start": "npm run watch & npm run server",
    "styles": "postcss -u postcss-import -u postcss-cssnext -c options.json -o dist/bundle.css src/styles/index.css",
    "compress": "cssnano < dist/bundle.css > dist/bundle.min.css",
    "server": "browser-sync start --files dist/bundle.css --server --no-notify --no-open",
    "watch": "npm run styles -- -w",
    "lint": "stylelint src/styles/*.css && xo src/scripts/*.js --space --env=browser",
    "test": "npm run lint"
  },
  "devDependencies": {
    "browser-sync": "^2.11.1",
    "cssnano-cli": "^1.0.3",
    "postcss-cli": "^2.5.1",
    "postcss-cssnext": "^2.4.0",
    "postcss-import": "^8.0.2",
    "stylelint": "^4.4.0",
    "xo": "^0.12.1"
  }
}
